{
  "buildCommand": "npm install --legacy-peer-deps && npm run build:vite",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "vite",
  "outputDirectory": "dist",
  "public": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}