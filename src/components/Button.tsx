/**
 * Backward-compatible Button re-export.
 * All components should migrate to `import { Button } from '@/components/ui/button'`.
 * This file maps the old API to the new unified shadcn-style Button.
 */
export { Button as default, Button, type ButtonProps } from '@/components/ui/button';

